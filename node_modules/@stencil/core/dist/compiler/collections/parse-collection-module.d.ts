import { Collection, CompilerCtx, Config, PackageJsonData } from '../../declarations';
export declare function parseCollectionModule(config: Config, compilerCtx: CompilerCtx, pkgJsonFilePath: string, pkgData: PackageJsonData): Collection;
