<pre><font color="#06989A">-- TYPE MISMATCH -------------------------------------------------- elm/Main.elm</font>

Something is off with the 1st branch of this `case` expression:

214|<font color="#CC0000">&gt;</font>            { title = &quot;Docket - All Tasks&quot;
215|<font color="#CC0000">&gt;</font>            , body = [ H.map TaskListMsg (TaskList.view subState appData environment) ]
216|<font color="#CC0000">&gt;</font>            }

The 1st branch is a record of type:

    { body : List <font color="#C4A000">(Html Msg)</font>, title : String }

But the type annotation on `view` says it should be:

    <font color="#C4A000">Browser.Document Msg</font>
</pre>






-- TYPE MISMATCH -------------------------------------------------- elm/Main.elm

Something is off with the 1st branch of this `case` expression:

214|>            { title = "Docket - All Tasks"
215|>            , body = [ H.map TaskListMsg (TaskList.view subState appData environment) ]
216|>            }

The 1st branch is a record of type:

    { body : List (Html Msg), title : String }

But the type annotation on `view` says it should be:

    Browser.Document Msg












When I try using a record constructor function




-- TYPE MISMATCH -------------------------------------------------- elm/Main.elm

The 2nd argument to `Document` is not what I expect:

214|             Browser.Document "_" [ H.map TaskListMsg (TaskList.view subState appData environment) ]
                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
This argument is a list of type:

    List (Html Msg)

But `Document` needs the 2nd argument to be:

    List (Html.Html msg)

Hint: I always figure out the argument types from left to right. If an argument
is acceptable, I assume it is “correct” and move on. So the problem may actually
be in one of the previous arguments!




<pre><font color="#06989A">-- TYPE MISMATCH -------------------------------------------------- elm/Main.elm</font>

The 2nd argument to `Document` is not what I expect:

214|             Browser.Document &quot;_&quot; [ H.map TaskListMsg (TaskList.view subState appData environment) ]
                                      <font color="#CC0000">^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^</font>
This argument is a list of type:

    List <font color="#C4A000">(Html Msg)</font>

But `Document` needs the 2nd argument to be:

    List <font color="#C4A000">(Html.Html msg)</font>

<u style="text-decoration-style:single">Hint</u>: I always figure out the argument types from left to right. If an argument
is acceptable, I assume it is “correct” and move on. So the problem may actually
be in one of the previous arguments!
</pre>
